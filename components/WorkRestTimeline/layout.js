// The lane label ("Work" / "Rest") and the spacers above it have to line up, so
// they all share one width. It narrows on small screens to leave the 96 columns
// as much room as possible.
export const LABEL_COLUMN = 'w-11 shrink-0 sm:w-16';

export const LANE_HEIGHT = 'h-[30px]';
